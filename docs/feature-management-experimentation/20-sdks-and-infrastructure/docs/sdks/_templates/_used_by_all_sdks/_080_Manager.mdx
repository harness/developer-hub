Use the FME Manager to get a list of feature flags available to the {props.sdk ?? 'SDK'} client. To instantiate a `Manager` in your codebase, use the same factory that you used for your client.

<>{props.codeSplitManagerInstantiate}</>

`Manager` public methods:

<>{props.codeSplitManager}</>

The `SplitView` object referenced above has the structure:

<>{props.codeSplitView}</>