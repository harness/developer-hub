The {props.sdk ?? 'SDK'} exposes configuration parameters that you can use to optimize {props.sdk ?? 'SDK'} performance. Each parameter is preset to a reasonable default. You can optionally override these default values when instantiating the {props.sdk ?? 'SDK'}.

<>{props.tableConfiguration}</>

To set each of the parameters defined above, use the following syntax:

<>{props.codeSplitClientConfig}</>

<>{props.configureLocalStorageCache}</>