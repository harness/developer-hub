import InitiateNewClient from './_001_InitiateNewClient.mdx';

## Initialization

To get started, set up Split in your codebase with the following two steps.

### 1. Add the package in your pubspec.yaml file

<>{props.codeImport}</>

### 2. Instantiate the plugin

<InitiateNewClient sdk='plugin' sdkKeyDetails={props.sdkKeyDetails} codeInitiate={props.codeInitiate} notes={props.notes} />