import InitiateNewClient from './_001_InitiateNewClient.mdx';

## Initialization

To get started, set up Split in your codebase with the following two steps.

### 1. Import the utilities into your project

<>{props.codeImport}</>

### 2. Instantiate the servide

<InitiateNewClient sdk='service' sdkKeyDetails={props.sdkKeyDetails} codeInitiate={props.codeInitiate} notes={props.notes} />