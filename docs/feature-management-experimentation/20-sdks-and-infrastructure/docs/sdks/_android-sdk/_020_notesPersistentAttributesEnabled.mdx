These attributes can be cached into the persistent memory cache of the SDK making them available for future sessions, as well as part of the `SDK_READY_FROM_CACHE` flow by setting the `persistentAttributesEnabled` to `true`.