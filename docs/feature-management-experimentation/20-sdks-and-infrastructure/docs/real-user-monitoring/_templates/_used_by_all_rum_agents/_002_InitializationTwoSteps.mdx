## Initialization {#initialization}

To get started, set up Harness FME RUM Agent in your code with the following steps.

### 1. Import the Agent into your project

<>{props.codeImport}</>

### 2. Set up the Agent

Configure the SDK with the client-side SDK key for the Split environment that you would like to access.

<>{props.sdkKeyDetails}</>

<>{props.codeInitiate}</>