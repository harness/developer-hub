## Custom events

Custom events can be tracked using the following methods: 
- using the `track` method

- using the specialized `trackError` method
{props.lastMethod} 

These methods are demonstrated below.

Using the `track` method: 

<>{props.codeTrack}</>

Using the `trackError` method:

<>{props.codeTrackError}</>

<>{props.codeCustomEvent}</>