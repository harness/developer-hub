## Flush {#flush}

The `flush` method sends the data stored in memory (impressions and events) to the Harness cloud and clears the successfully posted data. If a connection issue is encountered, the SDK re-attempts to send the data.

<>{props.extraInfo}</>

<>{props.example}</>