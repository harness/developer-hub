## Manager {#manager}

Use the FME Manager to get a list of feature flags available to the SDK client. To instantiate a `Manager` in your codebase, use the same factory that you used for your client.

<div>{props.instantiate}</div>

`Manager` public methods:

<div>{props.methods}</div>

The `SplitView` object referenced above has the structure:

<div>{props.splitView}</div>