/**
 * Module parser for Software Supply Chain Assurance (SCS).
 * Re-exports the scs-parser-extension so the registry can load it from parser.ts.
 */
export { parseSpec } from './scs-parser-extension';
